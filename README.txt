To run without compiling (OSX only, you will need to add Windows drivers if you want Windows):

````
cd bin/
java -jar LeapOSC.jar
````

The default port is 8000 on localhost.


Requires :

* OSCLib: https://github.com/odbol/OSCLib
* Leap Motion SDK: (included, add LeapJava to your build path)